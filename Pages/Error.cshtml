@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<div class="lock-overlay">
    <div class="lock-icon">
        <i class="bi bi-exclamation-triangle display-1 text-dark"></i>
    </div>
    <div class="lock-welcome text-dark">
        <h1 class="display-1 fw-bold text-dark">500</h1>
        <h1 class="mb-4 text-dark">Server Error</h1>
        <p class="mb-4 text-dark">
            @Model.ErrorMessage
        </p>
        @if (Model.ShowRequestId)
        {
            <p class="text-dark">
                <strong>Request ID:</strong> <code>@Model.RequestId</code>
            </p>
        }
    </div>
</div>


